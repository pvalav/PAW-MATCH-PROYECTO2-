<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Editar Mascota</title>
  <link rel="stylesheet" href="/estilos.css" />
</head>
<body>
 
  <main class="contenido">
    <h1>Editar Mascota</h1>

    <form action="/mascotas/editar/<%= mascota.id %>" method="POST">
      <label for="nombre">Nombre:</label>
      <input type="text" id="nombre" name="nombre" value="<%= mascota.nombre %>" required><br>

      <label for="edad">Edad:</label>
      <input type="number" id="edad" name="edad" value="<%= mascota.edad %>" required><br>

      <label for="tiempoEdad">Unidad de Edad:</label>
      <select name="tiempoEdad" id="tiempoEdad" required>
        <option value="meses" <%= mascota.tiempoEdad === 'meses' ? 'selected' : '' %>>Meses</option>
        <option value="años" <%= mascota.tiempoEdad === 'años' ? 'selected' : '' %>>Años</option>
      </select><br>

      <label for="sexo">Sexo:</label>
      <select name="sexo" id="sexo" required>
        <option value="Macho" <%= mascota.sexo === 'Macho' ? 'selected' : '' %>>Macho</option>
        <option value="Hembra" <%= mascota.sexo === 'Hembra' ? 'selected' : '' %>>Hembra</option>
      </select><br>

      <label for="caracteristicas">Características:</label>
      <textarea id="caracteristicas" name="caracteristicas" required><%= mascota.caracteristicas %></textarea><br>

      <label for="fecha">Fecha:</label>
      <input type="date" id="fecha" name="fecha" value="<%= mascota.fecha.toISOString().split('T')[0] %>" required><br>

      <button type="submit">Guardar cambios</button>
    </form>
  </main>
</body>
</html>